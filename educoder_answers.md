# 数组与函数

- 时间: 150分钟
- 截止: 2026-06-06T11:14:00.000+08:00
- 用户名: 赵俊涛 (312520030506)

---

## 单选题 (9题, 18.0分)

### Q1: (2.0分)

有以下程序
    #include<stdio. h>
	#include<string. h>
    int main()
    {   char p[]={'a', 'b', 'c'},q[10]={ 'a', 'b','c'};
        printf(''%d %d\n'',strlen(p),strlen(q));return 0;}
    以下叙述中正确的是()

- A) 在给p和q数组置初值时，系统会自动添加字符串结束符，故输出的长度都为3
- B) 由于p数组中没有字符串结束符，长度不能确定，但q数组中字符串长度为3
- C) 由于q数组中没有字符串结束符，长度不能确定，但p数组中字符串长度为3
- D) 由于p和q数组中都没有字符串结束符，故长度都不能确定

**解析**: 数组p赋初值时，没有字符串结束标识符，其长度不能确定，数组q部分初始化，没有赋值部分都是\0,因此长度为3。

---

### Q2: (2.0分)

当执行下面程序时，如果输入ABC，则输出结果是（）
    #include"stdio.h"
    #include"string.h"
    int main()
    {  char ss[10]="12345";
       gets(ss);strcat(ss,"6789"); printf("%s\n",ss);
	   return 0;
	}

- A) ABC6789
- B) ABC67
- C) 12345ABC6
- D) ABC456789

**解析**: 用gets给数组ss输入字符串时己取代了之前对ss数组的初始化值，strcat是连接函数，将输入的字符串与6789字符串连接，因此结果为ABC6789

---

### Q3: (2.0分)

以下程序的输出结果是()
#include"stdio.h"
    int main()
    {  char w[][10]={"ABCD","EFGH","IJKL","MNOP"};
       int k;
       for(k=1;k<3;k++)  printf("%s\n",w[k]);
	   return 0;
	}

- A) ABCD
FGH
KL
M
- B) ABCD
EFG
IJ
- C) EFG
JK
O
- D) EFGH
IJKL

**解析**: k从1开始循环到2结束，输出用格式符%s要，所以输出的是第二行和第三行

---

### Q4: (2.0分)

下列程序的输出结果是（）
    #include"stdio.h"
    #include"string.h"
    int main()
    {  char str[20]="hello\0\t\'||";
       printf("%d %d\n",strlen(str),sizeof(str));
	   return 0;
	}

- A) 9 9
- B) 5 20
- C) 13 20
- D) 20 20

**解析**: sstrlen函数是测字符串长度，遇到\0停止；sizeof是运算符，在此题是计算数组str在内存中所占字节数，所以是5 20

---

### Q5: (2.0分)

给出以下定义：
    char x[ ]="abcdefg";
    char y[ ]={ 'a', 'b', 'c', 'd', 'e', 'f', 'g'};
    则正确的叙述为（  ）。

- A) 数组x和数组y等价
- B) 数组x和数组y的长度相同
- C) 数组x的长度大于数组y的长度
- D) 数组x的长度小于数组y的长度

**解析**: 用字符串给数组赋初值时，系统对字符串常量自动加一个'\0'作为结束符。当用逐个字符给数组赋初值时，如果提供的初值个数与预定的数组长度相同，在定义时可以省略数组长度，系统会自动根据初值个数确定数组长度。

---

### Q6: (2.0分)

有定义语句：int b;char c[10];则正确的输入语句是（）

- A) scanf("%d%s",&b,&c);
- B) scanf("%d%s",&b,c);
- C) scanf("%d%s",b,c);
- D) scanf("%d%s",b,&c);

**解析**: 输入函数scanf地址列表中是地址，数组名是数组的首地址，因此B正确。

---

### Q7: (2.0分)

当执行下面程序且输入：ABC时，输出的结果是（）
#include<stdio.h>
#include<string.h>
int main( )
{  char ss[10]="12345";
   strcat(ss,"6789");
   gets(ss);printf("%s\n",ss);
   return 0;
}

- A) ABC
- B) ABC9
- C) 123456ABC
- D) ABC456789

**解析**: 内存中新值取代旧值。

---

### Q8: (2.0分)

下面程序的运行结果是（）
#include<stdio.h>
int main( )
    {  char  ch[7]={ "65ab21"};
       int  i,s=0;
       for(i=0;ch[i]>='0'&& ch[i]<='9';i+=2)
         s=10*s+ch[i]- '0';
       printf("%d\n",s);
   return 0;
}

- A) 12 ba56
- B) 6521
- C) 6
- D) 62

**解析**: 根据判断条件，for循环只循环一遍，a[i]的值是‘6’，运算‘6’-‘0’时转换为ASCII码的值运算结果为6

---

### Q9: (2.0分)

在每个 C程序中都必须包含的一个函数是

- A) <p>main()</p>
- B) <p>MAIN()</p>
- C) <p>name()</p>
- D) <p>function()</p>

**解析**: 暂无解析

---

## 编程题 (8题, 82.0分)

### Q10: (10.0分)

字符串排序

---

### Q11: (10.0分)

求三个实数中的最大值

---

### Q12: (10.0分)

计算汇款收费

---

### Q13: (10.0分)

判断整数n的各位数字中是否包含数字3或4

---

### Q14: (10.0分)

求具有10个数的数组中的最大值和次最大值

---

### Q15: (12.0分)

最大公约数

---

### Q16: (10.0分)

计算级数

---

### Q17: (10.0分)

求数列和

---

